import * as GLOBALS from './constants/globals';
const pageTitle = 'Home page';

document.addEventListener("DOMContentLoaded", function(event) {
    console.log(`DOM fully loaded on ${pageTitle}`);
});